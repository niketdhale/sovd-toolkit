# Copy to .env.local to change the default value pre-filled in the
# Connect bar. The base URL is still editable at runtime either way --
# nothing here is required.
VITE_SOVD_BASE_URL=http://localhost:20003
